CGK Handling PWA v16

Fixes in this build:
- Replaces missing Lucide dependency with internal inline SVG icons so all toolbar/icon buttons are visible.
- Uses a polished airplane logo image in the top-left brand area.
- Updates iPhone/PWA icons to the same clean airplane logo.
- Makes schedule modal X and Batal controls directly clickable with inline and delegated handlers.
- Raises modal z-index and disables pointer interception bugs.
- Keeps initial calendar rendering, editable schedules, paste import, file import, filters, export, print, and custom schedules.

Upload the CONTENTS of this folder to the repository root.
